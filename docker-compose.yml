services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - app_data:/app/data
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

volumes:
  app_data:
